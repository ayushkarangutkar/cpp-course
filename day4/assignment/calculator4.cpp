#include<iostream>
#include<math.h>
using namespace std;

class calculator
{
    double add(double a,double b)
    {
        double add;
        add=a+b;
        return (add);
    }
}; 
int main()
{
    calculator c1;
    double a,b,k;
    cout<<"Enter the two value"<<endl;
    cin>>a>>b;
    k=add(a,b);
    cout<<endl<
}
